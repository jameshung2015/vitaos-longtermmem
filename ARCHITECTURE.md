# 长期记忆应用架构文档

## 1. 项目概述

这是一个基于火山引擎和开源 mem0 框架构建的长期记忆应用，旨在解决传统 AI 对话"瞬时记忆"的局限性，通过抽取用户偏好、行为习惯等信息，实现跨周期对话中的认知连贯性。

### 1.1 核心能力
- **记忆提取**: 利用 DeepSeek-R1 从对话中提取关键事实
- **向量存储**: 通过 VikingDB 进行高效的向量化存储和检索
- **记忆增强**: 在对话时召回相关记忆，提供个性化回复
- **角色扮演**: 基于豆包 character 模型的角色对话能力

### 1.2 应用场景
- 结合偏好个性推荐：在推荐场景，会结合用户在饮食、兴趣、习惯上的偏好提出个性化建议
- 记忆用户关键日程：用户曾提及每周五会上瑜伽课，那么后续每逢周五，系统都会在对话中主动进行日程提醒
- 持续追踪学习轨迹：在教育辅导场景，如果学生在同一知识点反复出错，系统会有针对性进行强化训练

## 📋 文档目录

1. [项目概述](#1-项目概述) - 核心能力与应用场景
2. [系统架构](#2-系统架构) - 整体架构与数据流向
   - 2.1 整体架构图
   - 2.2 数据流向  
   - **2.3 数据存储架构** - 前端、后端、云端存储位置详解
3. [技术栈详解](#3-技术栈详解) - 前后端技术选型
4. [核心组件架构](#4-核心组件架构) - 代码结构分析
5. [核心技术实现](#5-核心技术实现) - 记忆提取、向量存储、对话机制
6. [依赖的开源方案](#6-依赖的开源方案) - Mem0、React、Python生态
7. [局域网部署评估](#7-局域网部署评估) - 部署可行性与方案对比
8. **[数据存储详细规划](#8-数据存储详细规划)** - 容量估算、性能要求、备份策略
   - 8.1 存储容量估算
   - 8.2 存储性能要求
   - 8.3 备份与恢复策略
   - 8.4 存储安全策略
   - 8.5 存储监控告警
9. [性能优化建议](#9-性能优化建议) - 前后端与数据库优化
10. [安全性考虑](#10-安全性考虑) - 数据安全与API安全
11. [监控和运维](#11-监控和运维) - 监控指标与告警策略
12. [总结](#12-总结) - 技术特点与部署建议

---

## 2. 系统架构

### 2.1 整体架构图
```
┌─────────────────────────────────────────────────────────────────┐
│                        火山引擎云服务                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────┐  │
│  │ DeepSeek-R1 │  │Doubao-pro-32k│  │Doubao-embed │  │VikingDB │  │
│  │  记忆提取   │  │   对话生成   │  │   向量化    │  │ 向量库  │  │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────┐    ┌─────────────────┐
│                 │    │                 │
│    前端应用     │◄──►│    后端服务     │
│   (React)       │    │   (Python)      │
│  - 对话界面     │    │  - API 路由     │
│  - 记忆展示     │    │  - 记忆管理     │
│  - 状态管理     │    │  - Prompt 处理  │
│                 │    │                 │
└─────────────────┘    └─────────────────┘
                              │
                              ▼
                       ┌─────────────────┐
                       │   Mem0 框架     │
                       │  (开源组件)     │
                       │ - 记忆抽象层    │
                       │ - 向量检索      │
                       │ - 配置管理      │
                       └─────────────────┘
```

### 2.2 数据流向
```
1. 记忆存储流程:
   用户对话 → 后端API → DeepSeek-R1提取事实 → Doubao-embedding向量化 → VikingDB存储

2. 记忆检索流程:
   用户查询 → 后端API → VikingDB向量搜索 → 召回相关记忆 → 增强Prompt → Doubao生成回复

3. 前端交互流程:
   用户输入 → React组件 → API调用 → SSE流式响应 → 实时UI更新
```

### 2.3 数据存储架构

#### 存储位置分布
```
┌─────────────────────────────────────────────────────────────────┐
│                     数据存储层架构                               │
│                                                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │   前端存储      │  │   后端存储      │  │   云端存储      │  │
│  │                 │  │                 │  │                 │  │
│  │ • 浏览器缓存    │  │ • 配置文件      │  │ • VikingDB     │  │
│  │ • LocalStorage  │  │ • 日志文件      │  │ • 模型服务     │  │
│  │ • SessionStorage│  │ • 临时文件      │  │ • API网关      │  │
│  │ • IndexedDB     │  │                 │  │ • 监控日志     │  │
│  │                 │  │                 │  │                 │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

#### 详细存储映射

##### 前端数据存储
| 数据类型 | 存储位置 | 持久化 | 用途 |
|---------|----------|--------|------|
| **聊天历史** | `Zustand Store` | 否 | 当前会话消息列表 |
| **用户配置** | `LocalStorage` | 是 | botId, userId, apiPath 配置 |
| **会话状态** | `SessionStorage` | 会话级 | 临时UI状态，页面刷新丢失 |
| **记忆缓存** | `IndexedDB` | 是 | 本地记忆数据缓存 |
| **首次提示** | `LocalStorage` | 是 | 用户首次使用标识 |

```typescript
// 前端存储位置示例
localStorage.setItem('longterm_memory_config', JSON.stringify({
  botId: 'user_bot_001',
  userId: 'user_001', 
  apiPath: 'http://localhost:8888/api/v3/bots/chat/completions'
}));

// Zustand 存储
useChatStore: {
  chatList: Message[],      // 内存中的聊天消息
  isReasoning: boolean      // 推理状态
}

useMemoryStore: {
  memoryList: Memory[],     // 内存中的记忆列表
  isLoading: boolean        // 加载状态
}
```

##### 后端数据存储
| 数据类型 | 存储位置 | 路径示例 | 用途 |
|---------|----------|----------|------|
| **配置文件** | `backend/code/config.py` | `/code/config.py` | API密钥、端点配置 |
| **Prompt模板** | `backend/code/prompt.py` | `/code/prompt.py` | 系统提示词模板 |
| **依赖配置** | `backend/pyproject.toml` | `/pyproject.toml` | Python依赖管理 |
| **本地包** | `backend/mem0ai-*.whl` | `/mem0ai-0.1.48.2-py3-none-any.whl` | Mem0框架本地包 |
| **运行时日志** | `系统临时目录` | `/tmp/arkitect_logs/` | 应用运行日志 |
| **进程文件** | `系统临时目录` | `/tmp/.arkitect_pid` | 进程管理文件 |

```python
# 后端存储位置示例
backend/
├── code/
│   ├── config.py           # 配置存储
│   ├── prompt.py           # 模板存储  
│   └── main.py            # 业务逻辑
├── mem0ai-0.1.48.2-py3-none-any.whl  # 依赖包存储
├── pyproject.toml         # 依赖配置
├── poetry.lock           # 锁定版本
└── .venv/                # 虚拟环境
    └── site-packages/    # 安装的包
```

##### 云端数据存储
| 服务名称 | 存储内容 | 存储位置 | 数据格式 |
|---------|----------|----------|----------|
| **VikingDB** | 用户记忆向量 | 华北云端集群 | 向量 + 元数据 |
| **API网关** | 请求日志 | 火山引擎日志服务 | JSON格式日志 |
| **函数服务** | 运行时日志 | 火山引擎日志服务 | 结构化日志 |
| **模型服务** | 推理缓存 | 火山引擎内部 | 临时缓存 |

```json
// VikingDB 数据结构示例
{
  "collection_name": "demohouse_mem0",
  "documents": [
    {
      "id": "mem_001", 
      "vector": [0.1, 0.2, ...],  // 2048维向量
      "metadata": {
        "user_id": "user_001",
        "agent_id": "bot_001", 
        "memory": "用户喜欢喝咖啡",
        "timestamp": "2025-06-17T10:00:00Z",
        "source": "chat_conversation"
      }
    }
  ]
}
```

##### 本地化部署存储位置
在局域网部署时，存储位置会发生变化：

**混合部署模式**:
```
本地存储:
├── frontend/build/        # 前端静态文件
├── backend/              # 后端服务文件
├── chroma_db/           # 本地向量数据库
│   ├── chroma.sqlite3   # SQLite数据库文件
│   └── index/           # 向量索引文件
└── logs/                # 本地日志文件

云端存储 (保留):
├── 火山引擎模型API      # DeepSeek-R1, Doubao服务
└── API网关日志         # 请求监控日志
```

**完全本地化模式**:
```
本地存储:
├── models/              # 本地模型文件
│   ├── llama3.1/       # 对话模型
│   ├── qwen2.5/        # 推理模型  
│   └── embedding/      # 嵌入模型
├── qdrant_data/        # 向量数据库
│   ├── collection/     # 向量集合
│   └── wal/           # 写前日志
├── redis_data/         # 缓存数据
└── logs/              # 所有日志文件
    ├── app.log        # 应用日志
    ├── model.log      # 模型推理日志
    └── db.log         # 数据库日志
```

## 3. 技术栈详解

### 3.1 前端技术栈

#### 核心框架
- **React 18.3.1**: 用户界面构建
- **Modern.js 2.65.3**: 全栈开发框架，提供开发工具链
- **TypeScript 5.7.3**: 类型安全的 JavaScript 超集

#### 状态管理
- **Zustand 5.0.3**: 轻量级状态管理库
  - `useChatStore`: 管理聊天消息状态
  - `useMemoryStore`: 管理记忆数据状态  
  - `useConfigStore`: 管理应用配置

#### UI 组件库
- **Arco Design 2.65.0**: 企业级 React UI 组件库
- **TailwindCSS**: 原子化 CSS 框架
- **CSS Modules**: 组件级样式隔离

#### 工具库
- **ahooks 3.8.4**: React Hooks 工具集
- **dayjs 1.11.13**: 轻量级日期处理库
- **uuid 11.1.0**: 唯一标识符生成
- **eventsource-parser 3.0.0**: SSE 事件流解析

#### 开发工具
- **Biome 1.9.4**: 快速的代码格式化和检查工具
- **PNPM**: 高效的包管理器

### 3.2 后端技术栈

#### 核心框架
- **Python 3.9-3.12**: 运行时环境
- **Arkitect**: 火山引擎自研的微服务框架
  - 提供 HTTP 服务能力
  - 支持异步处理
  - 内置监控和链路追踪

#### 依赖管理
- **Poetry 1.6.1**: Python 依赖管理和打包工具
- **Mem0 v0.1.48.2**: 核心记忆管理框架 (本地 wheel 包)
- **volcengine 1.0.164**: 火山引擎 SDK

#### 核心库
- **Jinja2**: 模板引擎，用于 Prompt 模板渲染
- **asyncio**: 异步编程支持

## 4. 核心组件架构

### 4.1 前端组件结构

```
frontend/src/demo/longTermMemory/
├── components/                  # UI 组件层
│   ├── Conversation/           # 对话组件
│   │   ├── index.tsx          # 主对话容器
│   │   ├── ChatList.tsx       # 消息列表
│   │   └── Welcome.tsx        # 欢迎界面
│   ├── MemoryHistory/         # 记忆历史组件
│   │   ├── index.tsx          # 记忆容器
│   │   ├── MemoryCard.tsx     # 单个记忆卡片
│   │   └── MemoryList.tsx     # 记忆列表
│   ├── ChatMessage/           # 消息组件
│   ├── MessageInput/          # 消息输入
│   ├── MemoryExtraction/      # 记忆提取展示
│   └── ActionIcon/            # 操作按钮
├── hooks/                      # 业务逻辑 Hooks
│   ├── useChat.ts             # 聊天相关逻辑
│   ├── useMemoryUpdate.ts     # 记忆更新逻辑
│   ├── useScrollToBottom.ts   # 滚动控制
│   └── useFirstTimeTooltip.ts # 首次使用提示
├── stores/                     # 状态管理
│   ├── useChatStore.ts        # 聊天状态
│   ├── useMemoryStore.ts      # 记忆状态
│   └── useConfigStore.ts      # 配置状态
├── utils/                      # 工具函数
│   ├── PostSSE.ts             # SSE 通信实现
│   └── fakeI18n.ts            # 国际化模拟
├── api.ts                      # API 接口封装
├── types.ts                    # TypeScript 类型定义
└── index.tsx                   # 主入口组件
```

### 4.2 后端服务架构

```
backend/code/
├── main.py                     # 主服务入口
│   ├── store_memory()         # 存储对话记忆
│   ├── chat_completions()     # 聊天补全 (带记忆)
│   ├── list_memory()          # 获取记忆列表
│   └── add_memory()           # 手动添加记忆
├── config.py                   # 配置管理
│   ├── 火山引擎 API 配置
│   ├── Mem0 框架配置
│   └── 向量数据库配置
└── prompt.py                   # Prompt 模板
    ├── SUMMARY_PROMPT         # 记忆提取模板
    ├── SYSTEM_PROMPT          # 系统角色设定
    └── CHAT_PROMPT            # 对话模板
```

## 5. 核心技术实现

### 5.1 记忆提取机制

#### Prompt 设计
```python
SUMMARY_PROMPT = """
你是一个个人信息管理器，专门负责准确存储事实、用户记忆和偏好。
需要记住的信息类型：
- 存储个人偏好：喜好、厌恶和特定偏好
- 维护重要个人细节：姓名、关系和重要日期
- 跟踪计划和意图：即将发生的事件、旅行、目标
- 记住活动和服务偏好：就餐、旅行、爱好
- 监控健康和健康偏好：饮食限制、健身计划
- 存储职业信息：职位、工作习惯、职业目标
"""
```

#### 提取流程
1. 用户对话输入
2. DeepSeek-R1 模型分析对话内容
3. 提取结构化事实信息 (JSON 格式)
4. 过滤无效信息 (问候语、语气词等)

### 5.2 向量存储与检索

#### 存储流程
```python
# 向量化配置
"embedder": {
    "provider": "doubao",
    "config": {
        "model": EMBEDDING_ENDPOINT,
    }
}

# 存储到 VikingDB
"vector_store": {
    "provider": "vikingdb", 
    "config": {
        "collection_name": "demohouse_mem0",
    }
}
```

#### 检索机制
1. 用户查询向量化
2. VikingDB 向量相似度搜索
3. 返回 Top-K 相关记忆
4. 记忆内容注入到对话 Prompt

### 5.3 角色对话系统

#### 角色设定
```python
SYSTEM_PROMPT = """
【你的身份】
方舟舟，一名刚刚毕业步入职场的26岁男生，星座狮子座，
温柔体贴，幽默风趣，特别爱聊天。心里装着每一位朋友，
总能精准地捕捉并记住人际交往中的每一个细节。

【我的信息】
{{session_memory}}

【要求】
1. 禁止将动作、神情语气放在对话（）中
2. 仅在问题与记忆信息非常相关时才参考记忆
"""
```

#### 对话增强
1. 检索相关记忆
2. 记忆内容注入系统 Prompt
3. Doubao-pro-32k 生成个性化回复
4. 流式返回对话结果

## 6. 依赖的开源方案

### 6.1 核心开源依赖

#### Mem0 框架 (Apache 2.0)
- **版本**: v0.1.48.2
- **作用**: 记忆管理核心框架
- **功能**:
  - 统一的记忆抽象层
  - 多种向量数据库支持
  - 记忆生命周期管理
  - 可插拔的 LLM 和 Embedding 提供商

#### React 生态 (MIT)
- **React**: 前端框架核心
- **Zustand**: 状态管理
- **Arco Design**: UI 组件库
- **Modern.js**: 开发框架

#### Python 生态
- **Jinja2** (BSD-3-Clause): 模板引擎
- **Poetry** (MIT): 依赖管理
- **asyncio**: 异步编程 (Python 内置)

### 6.2 火山引擎服务

#### 大模型服务
| 服务 | 作用 | 特点 |
|------|------|------|
| DeepSeek-R1 | 记忆提取 | 强化学习训练，推理能力强 |
| Doubao-pro-32k | 对话生成 | 32K 上下文，高质量回复 |
| Doubao-embedding | 向量化 | 2048维向量，中英双语 |

#### 基础设施服务
| 服务 | 作用 | 特点 |
|------|------|------|
| VikingDB | 向量数据库 | 高性能向量检索 |
| 函数服务 | 无服务器计算 | 弹性扩缩容 |
| API网关 | 服务路由 | 高可用负载均衡 |
| 日志服务 | 监控运维 | 实时日志分析 |

## 7. 局域网部署评估

### 7.1 可行性分析

#### ✅ 可本地部署
- **前端应用**: React 静态文件，可完全本地化
- **后端服务**: Python 服务，支持本地运行
- **Mem0 框架**: 开源框架，无外部依赖
- **业务逻辑**: 完整的应用逻辑可本地执行

#### ❌ 外部服务依赖
- **大模型 API**: 必须依赖火山引擎云服务
  - DeepSeek-R1 endpoint (记忆提取)
  - Doubao-pro-32k endpoint (对话生成)
  - Doubao-embedding endpoint (向量化)
- **向量数据库**: 当前配置依赖 VikingDB 云服务

### 7.2 本地化改造方案

#### 方案一：混合部署 (推荐)
保持核心业务逻辑本地化，通过网络访问必要的云服务。

**本地组件**:
```
├── 前端 React 应用 (本地)
├── 后端 Python 服务 (本地)
├── 本地向量数据库 (Chroma/Qdrant)
└── 本地文件存储
```

**外部依赖**:
```
├── 火山引擎大模型 API (通过网络)
└── 可选：本地代理缓存
```

**配置修改**:
```python
# config.py 修改向量数据库配置
mem0_config = {
    "vector_store": {
        "provider": "chroma",  # 改为本地数据库
        "config": {
            "collection_name": "local_memory",
            "path": "./chroma_db",
            "host": "localhost",
            "port": 8000
        }
    },
    # LLM 和 embedder 保持火山引擎配置
    "llm": {
        "provider": "doubao",
        "config": {"model": SUMMARY_ENDPOINT}
    },
    "embedder": {
        "provider": "doubao", 
        "config": {"model": EMBEDDING_ENDPOINT}
    }
}
```

#### 方案二：完全本地化
替换所有外部依赖为本地开源方案。

**替换方案**:
```
DeepSeek-R1 → 本地 Ollama + Qwen/ChatGLM
Doubao-pro-32k → 本地 Ollama + Llama3.1
Doubao-embedding → sentence-transformers
VikingDB → Chroma/Qdrant/Milvus
```

**资源需求**:
- **GPU**: RTX 4090 或更高 (用于本地模型推理)
- **内存**: 64GB+ (加载多个大模型)
- **存储**: 1TB+ SSD (存储模型文件)
- **CPU**: 16核以上

### 7.3 局域网部署步骤

#### 环境准备
```bash
# 1. Python 环境 (3.9-3.12)
python -m venv .venv
.venv\Scripts\activate  # Windows
# source .venv/bin/activate  # Linux/Mac

# 2. Node.js 环境 (18.0+)
node --version
pnpm --version

# 3. 本地向量数据库 (Chroma)
pip install chromadb
```

#### 修改配置
```python
# backend/code/config.py
import os

# 保持火山引擎认证
os.environ['VOLC_ACCESSKEY'] = '<YOUR_ACCESSKEY>'
os.environ['VOLC_SECRETKEY'] = '<YOUR_SECRETKEY>'

# 修改向量数据库为本地
mem0_config = {
    "vector_store": {
        "provider": "chroma",
        "config": {
            "collection_name": "local_longterm_memory",
            "path": "./local_chroma_db"
        }
    },
    "llm": {
        "provider": "doubao",
        "config": {"model": SUMMARY_ENDPOINT}
    },
    "embedder": {
        "provider": "doubao",
        "config": {"model": EMBEDDING_ENDPOINT}
    }
}
```

#### 安装依赖
```bash
# 后端依赖
cd backend
pip install poetry==1.6.1
poetry install
pip install chromadb  # 添加本地向量数据库

# 前端依赖  
cd ../frontend
pnpm install
```

#### 启动服务
```bash
# 启动后端 (端口 8888)
cd backend
python code/main.py

# 启动前端 (端口 8080)
cd frontend  
pnpm run dev
```

#### 网络配置
```bash
# 确保局域网可访问
# 前端: http://<local-ip>:8080
# 后端: http://<local-ip>:8888

# 防火墙开放端口
# Windows: 防火墙设置开放 8080, 8888 端口
# Linux: ufw allow 8080 && ufw allow 8888
```

### 7.4 部署架构对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **混合部署** | 部署简单，效果好，成本低 | 需要网络连接 | 临时演示，快速验证 |
| **完全本地** | 完全离线，数据安全 | 资源需求高，效果可能下降 | 保密环境，长期使用 |
| **云端部署** | 效果最佳，运维简单 | 依赖网络，成本较高 | 生产环境，大规模使用 |

### 7.5 局域网部署限制

#### 技术限制
1. **模型性能**: 本地开源模型效果可能不如商业模型
2. **计算资源**: 需要高性能硬件支撑本地模型推理
3. **网络延迟**: 混合部署模式下的 API 调用延迟
4. **存储容量**: 本地模型文件占用大量存储空间

#### 功能限制
1. **记忆质量**: 本地模型的记忆提取可能不够精准
2. **对话质量**: 开源对话模型可能缺乏个性化能力
3. **向量检索**: 本地向量数据库性能可能不如云服务
4. **扩展性**: 本地部署难以支撑大并发访问

#### 推荐配置 (完全本地化)
```
硬件配置:
├── CPU: Intel i9-13900K 或 AMD R9-7950X
├── GPU: RTX 4090 24GB (推理) + RTX 4060 16GB (嵌入)
├── 内存: 128GB DDR5
├── 存储: 2TB NVMe SSD (系统) + 4TB SSD (模型)
└── 网络: 千兆以太网

软件配置:
├── 操作系统: Ubuntu 22.04 LTS
├── 容器: Docker + Docker Compose
├── 模型服务: Ollama + Open WebUI
├── 向量数据库: Qdrant (Docker)
└── 监控: Prometheus + Grafana
```

## 8. 数据存储详细规划

### 8.1 存储容量估算

#### 单用户数据量估算
| 数据类型 | 单条大小 | 预估条数/天 | 日增长量 | 月增长量 |
|---------|----------|-------------|----------|----------|
| **对话消息** | 500B | 100条 | 50KB | 1.5MB |
| **提取记忆** | 200B | 10条 | 2KB | 60KB |
| **向量数据** | 8KB | 10条 | 80KB | 2.4MB |
| **元数据** | 300B | 10条 | 3KB | 90KB |
| **总计** | - | - | **135KB/天** | **4MB/月** |

#### 多用户场景估算 (1000用户)
| 时间周期 | 数据增长量 | 累计存储量 | 推荐存储空间 |
|---------|------------|------------|-------------|
| **1天** | 135MB | 135MB | 500MB |
| **1周** | 945MB | 945MB | 2GB |
| **1月** | 4GB | 4GB | 10GB |
| **1年** | 48GB | 48GB | 100GB |

### 8.2 存储性能要求

#### 读写性能指标
| 操作类型 | QPS需求 | 延迟要求 | 存储类型推荐 |
|---------|---------|----------|-------------|
| **记忆写入** | 10/s | <500ms | SSD |
| **向量检索** | 50/s | <100ms | NVMe SSD |
| **聊天记录** | 100/s | <50ms | 内存缓存 |
| **配置读取** | 1/s | <10ms | 本地文件 |

#### 存储IOPS需求
```
向量数据库 (VikingDB/Chroma):
├── 随机读: 1000 IOPS
├── 随机写: 100 IOPS  
├── 顺序读: 100 MB/s
└── 顺序写: 50 MB/s

应用数据:
├── 配置文件: 10 IOPS
├── 日志文件: 50 IOPS
└── 缓存数据: 500 IOPS
```

### 8.3 备份与恢复策略

#### 数据备份策略
| 数据类型 | 备份频率 | 备份方式 | 保留周期 |
|---------|----------|----------|----------|
| **用户记忆** | 每日 | 增量备份 | 3个月 |
| **配置文件** | 每次修改 | 版本控制 | 永久 |
| **向量索引** | 每周 | 全量备份 | 1个月 |
| **聊天日志** | 每日 | 压缩备份 | 1周 |

#### 恢复方案
```bash
# 1. 向量数据库恢复
# Chroma 恢复
cp -r backup/chroma_db_20250617/ ./chroma_db/

# Qdrant 恢复  
docker run -v backup_vol:/qdrant/storage qdrant/qdrant:latest

# 2. 配置文件恢复
git checkout config_backup_tag
cp config/config.py.backup backend/code/config.py

# 3. 用户数据恢复
python scripts/restore_user_data.py --backup-date 2025-06-17
```

### 8.4 存储安全策略

#### 数据加密
| 存储位置 | 加密方式 | 密钥管理 | 说明 |
|---------|----------|----------|------|
| **本地文件** | AES-256 | 本地密钥文件 | 配置文件加密 |
| **向量数据库** | TLS传输 + 存储加密 | 数据库内置 | 敏感记忆加密 |
| **云端存储** | 火山引擎内置加密 | 云端KMS | 自动管理 |
| **网络传输** | HTTPS/TLS 1.3 | SSL证书 | 端到端加密 |

#### 访问控制
```python
# 数据访问隔离策略
def get_user_memories(user_id: str, agent_id: str):
    """严格按用户ID隔离数据访问"""
    return memory_client.search(
        query=query,
        user_id=user_id,      # 必须提供用户ID
        agent_id=agent_id,    # 必须提供代理ID
        limit=retrieve_count
    )

# 敏感信息脱敏
def sanitize_memory(memory_text: str) -> str:
    """对敏感信息进行脱敏处理"""
    # 手机号脱敏: 138****1234
    memory_text = re.sub(r'(\d{3})\d{4}(\d{4})', r'\1****\2', memory_text)
    # 身份证脱敏: 110***********1234
    memory_text = re.sub(r'(\d{3})\d{11}(\d{4})', r'\1***********\2', memory_text)
    return memory_text
```

### 8.5 存储监控告警

#### 关键监控指标
```yaml
监控配置:
  存储空间:
    - 磁盘使用率 > 80% (警告)
    - 磁盘使用率 > 90% (严重)
    - 可用空间 < 10GB (严重)
  
  性能指标:
    - 向量检索延迟 > 500ms (警告)
    - 数据写入失败率 > 1% (警告)
    - IOPS使用率 > 80% (警告)
  
  数据完整性:
    - 向量数据损坏检测 (每日)
    - 备份验证 (每周)
    - 数据一致性检查 (每月)
```

#### 自动化运维脚本
```bash
#!/bin/bash
# storage_health_check.sh

# 检查磁盘空间
df -h | awk '$5 > 80 {print "WARNING: " $1 " is " $5 " full"}'

# 检查向量数据库状态
curl -f http://localhost:8000/health || echo "Vector DB health check failed"

# 清理过期日志 (保留7天)
find ./logs -name "*.log" -mtime +7 -delete

# 数据库压缩 (Chroma)
python -c "
import chromadb
client = chromadb.PersistentClient(path='./chroma_db')
client.reset()  # 仅在维护窗口执行
"
```

## 9. 性能优化建议

### 9.1 前端优化
1. **代码分割**: 使用 React.lazy 和 Suspense 实现组件懒加载
2. **状态优化**: Zustand 状态细粒度拆分，避免不必要的重渲染
3. **请求优化**: SSE 连接复用，避免频繁建立连接
4. **缓存策略**: 记忆数据本地缓存，减少重复请求

### 9.2 后端优化
1. **异步处理**: 充分利用 Python asyncio 异步能力
2. **连接池**: 数据库连接池管理，提高并发性能
3. **缓存层**: Redis 缓存热点记忆数据
4. **批处理**: 批量处理记忆存储和检索请求

### 9.3 数据库优化
1. **索引优化**: 向量数据库索引参数调优
2. **分片策略**: 大规模数据的分片存储策略
3. **定期清理**: 过期记忆数据的清理机制
4. **备份恢复**: 定期数据备份和恢复方案

## 10. 安全性考虑

### 10.1 数据安全
1. **敏感信息**: 用户个人信息的脱敏处理
2. **访问控制**: 基于用户 ID 的记忆数据隔离
3. **传输加密**: HTTPS 加密所有 API 通信
4. **存储加密**: 向量数据库数据加密存储

### 10.2 API 安全
1. **身份认证**: 火山引擎 API 密钥管理
2. **权限控制**: API 调用频率限制
3. **输入验证**: 防止 Prompt 注入攻击
4. **审计日志**: 详细的操作日志记录

## 11. 监控和运维

### 11.1 监控指标
1. **业务指标**: 记忆提取成功率、对话响应时间
2. **系统指标**: CPU、内存、磁盘、网络使用率
3. **API 指标**: 调用次数、错误率、延迟分布
4. **用户指标**: 活跃用户数、会话时长

### 11.2 告警策略
1. **错误告警**: API 调用失败率超过阈值
2. **性能告警**: 响应时间超过预设值
3. **资源告警**: 系统资源使用率过高
4. **业务告警**: 记忆提取质量下降

## 12. 总结

### 12.1 技术特点
1. **现代化架构**: 基于 React + Python 的前后端分离架构
2. **云原生能力**: 充分利用火山引擎云服务优势
3. **开源整合**: 有效整合 Mem0 等开源组件
4. **可扩展性**: 模块化设计，易于功能扩展

### 12.2 部署建议
对于**临时局域网部署**，推荐采用**混合部署模式**：
- ✅ 前后端服务本地化部署
- ✅ 使用本地向量数据库 (Chroma)
- ✅ 保持火山引擎 API 用于模型服务
- ✅ 通过网络代理访问必要的云服务

### 12.3 发展方向
1. **模型本地化**: 逐步支持更多开源模型选择
2. **功能增强**: 增加记忆管理、冲突处理等高级功能
3. **性能优化**: 提升大规模部署下的性能表现
4. **生态扩展**: 支持更多向量数据库和 LLM 提供商

---

## 📊 附录：存储位置快速参考

### A.1 当前云端部署存储位置

| 数据类型 | 存储位置 | 路径/服务 | 持久化 |
|---------|----------|-----------|--------|
| **前端状态** | 浏览器 | localStorage/sessionStorage | 会话级 |
| **用户记忆** | VikingDB | 华北云端集群 | 永久 |
| **配置文件** | 本地文件 | `backend/code/config.py` | 永久 |
| **运行日志** | 云端服务 | 火山引擎日志服务 | 7天 |
| **模型缓存** | 云端服务 | 火山引擎内部缓存 | 临时 |

### A.2 混合部署存储位置 (推荐)

| 数据类型 | 存储位置 | 路径/服务 | 备注 |
|---------|----------|-----------|------|
| **前端应用** | 本地服务器 | `http://localhost:8080` | 静态文件 |
| **后端服务** | 本地服务器 | `http://localhost:8888` | Python进程 |
| **用户记忆** | 本地数据库 | `./chroma_db/` | SQLite+向量索引 |
| **模型API** | 云端服务 | 火山引擎API | 网络访问 |
| **应用日志** | 本地文件 | `./logs/app.log` | 本地轮转 |

### A.3 完全本地化存储位置

| 数据类型 | 存储位置 | 路径/服务 | 资源需求 |
|---------|----------|-----------|---------|
| **对话模型** | 本地文件 | `./models/llama3.1/` | 15GB |
| **推理模型** | 本地文件 | `./models/qwen2.5/` | 10GB |
| **嵌入模型** | 本地文件 | `./models/embedding/` | 2GB |
| **向量数据** | 本地数据库 | `./qdrant_data/` | 按使用量增长 |
| **缓存数据** | 本地Redis | `./redis_data/` | 内存映射 |

### A.4 存储空间规划建议

#### 开发环境 (单用户)
```
磁盘需求: 50GB
├── 系统与依赖: 10GB
├── 本地数据库: 5GB  
├── 日志文件: 5GB
└── 备用空间: 30GB
```

#### 生产环境 (1000用户)
```
磁盘需求: 500GB
├── 应用文件: 50GB
├── 用户数据: 100GB
├── 向量索引: 200GB
├── 日志备份: 50GB
└── 扩展空间: 100GB
```

#### 完全本地化 (包含模型)
```
磁盘需求: 1.5TB
├── 模型文件: 100GB
├── 用户数据: 200GB
├── 向量数据: 500GB
├── 系统缓存: 200GB
└── 备份空间: 500GB
```

---

**文档版本**: v1.1  
**更新日期**: 2025年6月17日  
**维护团队**: AI应用实验室开发团队  
**重点更新**: 补充数据存储位置详细规划
